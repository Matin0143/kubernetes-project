app.py

from flask import Flask
import requests

app = Flask(__name__)

@app.route("/")
def home():
    return "Flask Frontend is running on Kubernetes 🚀"

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000)

Dockerfile (Flask)
FROM python:3.9
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "app.py"]
